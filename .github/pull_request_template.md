**What this changes**

**Why**

**How to see it** — the page or situation a reviewer should try.

---

- [ ] Behaviour change comes with a test (`cd test && npm test`)
- [ ] `bridge.test.mjs` and `e2e.test.mjs` both pass locally
- [ ] Extension reloaded and tried by hand, if it touches `extension/`
